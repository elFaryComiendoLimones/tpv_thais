{% extends 'sidebar.html.twig' %}
{% block content %}

    {% if confirmed is defined and confirmed is not empty %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <strong>Provedor creado!</strong> El proveedor se ha insertado correctamente
        </div>
    {% endif %}

    <div class="card rounded">
        <div class="card-header card-header-rose card-header-icon d-flex align-items-center bg-white">
            <div class="card-icon w-25">
                <h4 class="card-title m-0 ml-2">Añadir proveedor</h4>
            </div>
        </div>
        <div class="card-body">
            {{ form_start(form, {'action': path('create-provider'), 'method': 'POST'}) }}
            <div class="form-group">
                {{ form_label(form.name) }}
                {{ form_widget(form.name) }}
            </div>
            <div class="form-group">
                {{ form_label(form.telefono) }}
                {{ form_widget(form.telefono) }}
            </div>
            <div class="form-group">
                {{ form_label(form.email) }}
                {{ form_widget(form.email) }}
            </div>
            <div class="form-group">
                {{ form_label(form.town) }}
                {{ form_widget(form.town) }}
            </div>

            <div class="form-group">
                {{ form_label(form.postcode) }}
                {{ form_widget(form.postcode) }}
            </div>
            <div class="form-group">
                {{ form_label(form.address) }}
                {{ form_widget(form.address) }}
            </div>
            <div class="form-group">
                {{ form_widget(form.save) }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>

    <a class="btn btn-dark mt-2" href="{{ path('providers') }}">Volver</a>
{% endblock %}